apiVersion: v1
kind: Service
metadata:
  name: vectorflow-inference
  namespace: vectorflow
  labels:
    app: vectorflow
    component: inference
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
  selector:
    app: vectorflow
    component: inference
